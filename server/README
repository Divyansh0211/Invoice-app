# Server Side

This directory contains the backend code for the BillSphere.

## Getting Started

1.  Install dependencies:
    ```bash
    npm install
    ```

2.  Run the server:
    ```bash
    npm start
    ```
